<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 搜索图标 -->
    <ImageView
        android:id="@+id/ivSearchIcon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="12dp"
        android:src="@drawable/ic_search"
        android:contentDescription="搜索"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 顶部 Tab 栏 - 允许横滑 -->
     <HorizontalScrollView
         android:id="@+id/top_tab_bar"
         android:layout_width="0dp"
         android:layout_height="wrap_content"
         android:scrollbars="none"
         app:layout_constraintTop_toTopOf="parent"
         app:layout_constraintStart_toStartOf="parent"
         app:layout_constraintEnd_toStartOf="@id/ivSearchIcon">

        <LinearLayout
            android:id="@+id/top_tab_bar_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp">

            <!-- Tab 列表：北京、团购、关注、社区、推荐 -->
            <TextView style="@style/TopTabTextStyle"
                android:text="北京" android:id="@+id/tvTabBeijing" />

            <TextView style="@style/TopTabTextStyle"
                android:text="团购" android:id="@+id/tvTabGroupBuy" />

            <TextView style="@style/TopTabTextStyle"
                android:text="关注" android:id="@+id/tvTabFollow" />

            <!-- 默认选中：社区 (使用 TopTabTextStyle.Selected 样式) -->
            <TextView style="@style/TopTabTextStyle.Selected"
                android:text="社区" android:id="@+id/tvTabCommunity" />

            <TextView style="@style/TopTabTextStyle"
                android:text="推荐" android:id="@+id/tvTabRecommend" />

        </LinearLayout>
    </HorizontalScrollView>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/top_tab_bar"
        app:layout_constraintBottom_toBottomOf="parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:clipToPadding="false" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Loading / Error State Views can be added here -->
</androidx.constraintlayout.widget.ConstraintLayout>
